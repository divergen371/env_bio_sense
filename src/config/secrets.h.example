#pragma once

// ご自身のWi-FiルーターのSSIDとパスワードに書き換えてください。
// (元のsecrets.hは.gitignoreに追加され、このファイルはテンプレートとしてコミットされます)// Wi-Fi Credentials for NTP sync and API access
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// GNSSを取得できない場合だけ使用する位置fallback。
// 使用する場合は1へ変更し、有効な10進度の座標を設定してください。
#define LOCATION_FALLBACK_ENABLED 0
#define LOCATION_LAT "0"
#define LOCATION_LON "0"
